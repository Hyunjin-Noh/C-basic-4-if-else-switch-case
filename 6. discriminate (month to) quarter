#include <stdio.h>

int main()
{
    int month;
    scanf("%d", &month);
    
    if (month<=3) printf("1분기입니다.");
    else if (month<=6) printf("2분기입니다.");
    else if (month<=9) printf("3분기입니다.");       
    else printf("4분기입니다.");    

    return 0;
}
